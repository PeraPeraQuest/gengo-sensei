#!/usr/bin/env bash
# start-docker-mongo

source dependencies.sh

# run an in-memory MongoDB container for testing
docker run \
    --detach \
    --name gengo-sensei-mongo-ram \
    --publish 27017:27017 \
    --rm \
    --tmpfs /data/db:rw,size=256m \
    mongo:${MONGODB_VERSION}
